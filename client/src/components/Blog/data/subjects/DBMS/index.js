// DBMS subject - add your topics here by importing them
import introToDatabases from "./intro-to-databases";

const DBMS = {
  id: "dbms",
  name: "DBMS",
  topics: [introToDatabases],
};

export default DBMS;
